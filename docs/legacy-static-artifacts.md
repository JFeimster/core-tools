# Legacy Static Artifacts

`landing.html`, `embed.html`, and duplicate `landing/index.html` are legacy root-level surfaces.

They should be treated as deprecated migration files and replaced by `tools/[slug]/landing/` and `tools/[slug]/embed/` artifacts.

