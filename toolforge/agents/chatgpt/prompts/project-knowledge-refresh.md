# Prompt: Project Knowledge Refresh

Refresh the Core Tools project knowledge pack from the latest repo state.

Use the current branch, the snapshot file, the main docs, and the ToolForge pack docs as sources. Exclude generated artifacts, write down any stale references, and return a compact update that a future thread can use without rereading the whole repo.
