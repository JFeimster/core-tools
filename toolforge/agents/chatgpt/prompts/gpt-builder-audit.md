# Prompt: GPT Builder Audit

Audit this ChatGPT GPT spec for Core Tools.

Check whether the purpose is specific, whether the knowledge sources are current, whether the instructions avoid runtime assumptions, and whether the test prompts would catch scope drift. Call out anything that should stop the build or be pushed to a later batch. Return the audit as a short list of findings, risks, and next edits.
