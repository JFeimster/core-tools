# Prompt: Bugfix With Tests

Fix the bug in the smallest safe way, then add the lightest test or validation step that proves the bug is gone.

Keep the scope narrow, avoid unrelated refactors, and stop if the fix requires architecture changes or runtime dependencies the repo does not use.
