# Prompt: UI Polish Pass

Perform a focused UI polish pass on the current static page.

Improve clarity, spacing, hierarchy, and CTA emphasis without changing the route model or introducing client-side state that the repo does not already use.
